{
  "name": "my-website",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "//": "When we develop locally we prefer to see the next branch",
    "start": "docusaurus start --no-open & open http://localhost:3000/yoshi/docs/next/api/configuration",
    "build": "docusaurus build",
    "version": "docusaurus docs:version",
    "publish-gh-pages": "docusaurus deploy",
    "publish-surge": "npx teamcity-surge-autorelease --dist=build --verbose"
  },
  "dependencies": {
    "@docusaurus/core": "^2.0.0-alpha.50",
    "@docusaurus/preset-classic": "^2.0.0-alpha.50",
    "classnames": "^2.2.6",
    "react": "^16.8.4",
    "react-dom": "^16.8.4"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
